<Page x:Class="POPD_WPF.PageAft"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:POPD_WPF"
      mc:Ignorable="d" 
      d:DesignHeight="460" d:DesignWidth="700"
      Title="PageAft">

    <Border Background="{StaticResource MaterialDesignLightBackground}" Margin="40" CornerRadius="20" >
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" Color="Black" ShadowDepth="0"/>
        </Border.Effect>
        <Grid>
            <Button VerticalAlignment="Top" HorizontalAlignment="Left" Margin="20" Content="Авторизация" x:Name="ButtonAft" IsEnabled="False" md:ButtonAssist.CornerRadius="10"/>
            <Button VerticalAlignment="Top" HorizontalAlignment="Right" Margin="20" Content="Регистрация" x:Name="ButtonReg" md:ButtonAssist.CornerRadius="10" Click="ButtonReg_Click"/>
            <TextBox VerticalAlignment="Center" md:HintAssist.Hint="Логин" Margin="100 0 100 80" x:Name="TextBoxLogin"/>
            <PasswordBox VerticalAlignment="Center" md:HintAssist.Hint="Пароль" Margin="100 0 100 0" x:Name="PassBoxPass"/>
            <Button VerticalAlignment="Bottom" Margin="20" md:ButtonAssist.CornerRadius="10" Content="Войти" x:Name="ButtonConfirmAft" Click="ButtonConfirmAft_Click"/>
        </Grid>
    </Border>
</Page>
