<Page x:Class="POPD_WPF.PageListUsers"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:POPD_WPF"
      mc:Ignorable="d" 
      d:DesignHeight="460" d:DesignWidth="700"
      Title="PageAft" x:Name="PageListUser">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="Gray" CornerRadius="20">
            <Grid>
                <TextBlock Text="Вы вошли как:" Margin="10 20" FontSize="20" FontWeight="DemiBold" x:Name="TBLoginInfo" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                <TextBox VerticalAlignment="Center" HorizontalAlignment="Right" md:HintAssist.Hint="Общая фильтрация" Margin="10" TextChanged="TBFilter_TextChanged"  x:Name="TBFilter"/>
            </Grid>
        </Border>

        <ListView x:Name="ListViewUsers" Grid.Row="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="Gray" CornerRadius="20" Width="200" Height="100" x:Name="BorderItemEl" BorderThickness="2" BorderBrush="Gray">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" Color="Black" ShadowDepth="0" />
                        </Border.Effect>
                        <StackPanel Margin="10" Orientation="Vertical">
                            <TextBlock Text="{Binding Name, StringFormat={}Имя: {0}}"/>
                            <TextBlock Text="{Binding Login, StringFormat={}Логин: {0}}" Margin="0 5 0 0"/>
                            <TextBlock Text="{Binding Password, StringFormat={}Пароль: {0}}" Margin="0 5 0 0"/>
                        </StackPanel>
                    </Border>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsCurUser}" Value="True">
                            <Setter  TargetName="BorderItemEl" Property="BorderBrush" Value="Lime"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            
        </ListView>
    </Grid>
</Page>
